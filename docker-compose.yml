services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    # Los puertos se manejan automáticamente por Easypanel
    # Las variables de entorno se configuran en Easypanel, no aquí
    environment:
      - NODE_ENV=production
    restart: unless-stopped

