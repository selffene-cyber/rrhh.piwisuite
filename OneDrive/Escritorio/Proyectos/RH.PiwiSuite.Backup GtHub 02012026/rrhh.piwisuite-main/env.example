# Copia este archivo a `.env.local` y completa los valores reales.
# En Windows (PowerShell):
#   Copy-Item env.example .env.local
#
# En macOS/Linux:
#   cp env.example .env.local

# --- Supabase (REQUERIDO para usar la app) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Solo servidor (opcional; necesario para endpoints admin) ---
SUPABASE_SERVICE_ROLE_KEY=

# --- Sitio (opcional) ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Cron (opcional) ---
CRON_SECRET=

# --- Gemini AI (opcional; necesario para el Asistente IA) ---
GEMINI_API_KEY=
# Modelo a usar: gemini-2.5-flash (recomendado, disponible en @google/genai)
# Alternativas: gemini-3-pro-preview
# Por defecto usa gemini-2.5-flash
GEMINI_MODEL=gemini-2.5-flash

